# Diary Whisper iOS 框架（不入库）

运行 `npm run fetch-diary-whisper`（或 `npm run build:ios`）后，本目录应有：

- `whisper.xcframework`（仅 ios-arm64 + 模拟器切片）

来源：whisper.cpp `whisper-v1.8.3-xcframework.zip`。  
最低系统：**iOS 16.4+**（上游框架要求）。
