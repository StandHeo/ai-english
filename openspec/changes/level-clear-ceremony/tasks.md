## 1. 通关仪式 UI 与结算时机

- [ ] 1.1 在 `LevelPage`（或新组件）增加通关仪式 phase/视图：压暗、大贴纸、星星反馈、居中大圆 ▶、左上退出
- [ ] 1.2 主线最后一拍成功后进入仪式；此时调用 `completeLevel`/发贴纸（防重入），移除约 1.2s 自动 `navigate`
- [ ] 1.3 实现贴纸图兜底：`reward.stickerImage` → 主词物品图 → 通用占位
- [ ] 1.4 通关 TTS：Cheer 池轮换 + `{Word}` 拼成单句；无主词则 `Yay!`
- [ ] 1.5 大圆 ▶：无确认，按既有规则导航下一关或地图
- [ ] 1.6 退出回地图：轻确认（✓/✕）；确认后回地图且保留已结算进度
- [ ] 1.7 久等（约 20–30s）仅 pulse ▶，永不自动跳

## 2. Beep 后置与可选入口

- [ ] 2.1 有 `beep_talk` 时末拍**不再**直接进 Beep；先仪式
- [ ] 2.2 仪式上增加小于 ▶ 的 Beep 小头像入口；无 `beep_talk` 不渲染
- [ ] 2.3 点小头像进入 `BeepTalkPanel`；`onComplete` 回到仪式（不自动下一关、不重复结算）
- [ ] 2.4 点 ▶ 时若未玩 Beep则静默跳过
- [ ] 2.5 Beep 中途退出：回仪式或轻确认回地图；不撤销贴纸/通关

## 3. 样式与横屏

- [ ] 3.1 仪式样式：大贴纸、大圆热区（≥72px 量级）、小 Beep 右下低对比
- [ ] 3.2 矮横屏：主按钮可改为底栏全宽，仍保持单一主行动

## 4. 文档与验收

- [ ] 4.1 更新 `docs/beep-tail-talk.md`「当前实现」与 `docs/parent-child-ux-notes.md` 实现状态
- [ ] 4.2 冒烟：无 Beep 关 → 仪式 → ▶ 下一关；有 Beep → 仪式可跳过或进 Beep 后回仪式再 ▶；退出轻确认不丢贴纸
