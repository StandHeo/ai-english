## 1. 消息存储与迁移

- [ ] 1.1 扩展 `FamilyDayRecord`：增加 `messages[]`（id / createdAt / text / audio），读写兼容旧数据
- [ ] 1.2 实现追加文字/语音消息、编辑文本、按日读取；由 messages 顺序合并更新 `story` 供生成
- [ ] 1.3 旧日仅有 `story` 时打开迁移为一条文字消息

## 2. 端侧 Whisper 适配层

- [ ] 2.1 新增日记 ASR 抽象（`isAvailable` / `transcribe`）；Web 实现标记不可用
- [ ] 2.2 Capacitor 原生桥接端侧 Whisper（tiny/int8 或等价），模型内置或首次解包
- [ ] 2.3 转写失败/模型未就绪时保留音频并返回可展示错误；不默认走云端 OpenAI

## 3. 聊天式制作台 UI

- [ ] 3.1 重做 `FamilyStudioPage`：按日气泡列表、文字发送、语音录制发送、气泡回放与文本编辑
- [ ] 3.2 DeepSeek Key / 生成关卡 / 选图收入折叠「生成与设置」区，不占聊天第一屏
- [ ] 3.3 无端侧 Whisper 时展示降级文案，纯文字路径仍可用

## 4. 日历与文档

- [ ] 4.1 日历/日详情对「有语音消息」做轻量可发现标记（不重做整页视觉）
- [ ] 4.2 文档：端侧模型、APK 验证、与关卡 Vosk 隔离说明
- [ ] 4.3 冒烟：文字气泡 → 合并生成；有 APK 时再验录音转写与杀进程回放
