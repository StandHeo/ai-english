## 1. 图标包与搜索模块

- [x] 1.1 增加构建脚本：从 `@iconify/json` 导出精简 `mdi` 包（控制体积）到 `apps/web` 可 import 的 JSON
- [x] 1.2 实现本地搜索：按关键词匹配图标名/别名，返回最佳结果
- [x] 1.3 实现 SVG → data URL 渲染（浅底正方形，适关卡点选）
- [x] 1.4 从关卡快照提取槽位关键词并批量生成最多 4 张 data URL

## 2. 家庭日记 UI 与设置

- [x] 2.1 设置页增加「生成后自动图标配图」开关（默认关）；若与通义自动同时开则优先图标并提示
- [x] 2.2 日记页增加「图标配图」按钮；写入 `images[]`（填空位 / 有图时确认替换）
- [x] 2.3 生成关卡成功后：若开启自动图标则本地配图并展示状态

## 3. 文档与验收

- [x] 3.1 补充文档：来源许可证、子集范围、与通义/相册关系
- [x] 3.2 手工验收：离线可图标配图；无匹配保留关卡；不覆盖未确认的相册图
